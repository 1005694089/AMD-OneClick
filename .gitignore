k8s-deployment-v2.local.yaml
k8s-manager-v2-manager-only.local.yaml
k8s-manager-v2-test.local.yaml
local-deploy-history/
cluster.yaml
external-url-dump.txt
app/__pycache__/
*.pyc

# operator runbook (local notes, not source)
image-service-node-0042-setup.md

# local env files with real secrets (OAuth, session, admin, DB)
.env.local
.env.*.local

# signing keys / credentials — never commit
secrets/
*.pem
